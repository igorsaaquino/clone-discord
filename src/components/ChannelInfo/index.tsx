import React from "react";

import { Container, Title, ExpandIcon } from './styles';

const ChannelInfo: React.FC = () => {
    return (
    <Container>
        <Title>Dev's Misticos</Title>
        <ExpandIcon />
    </Container>
    );
};

export default ChannelInfo; 